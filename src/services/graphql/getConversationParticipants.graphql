query GetConversation($conversationId: String!) {
  conversation(conversationId: $conversationId) {
    id
    admin {
      userId
      deviceId
    }
    adminUserIds
    type
    participants {
      userId
      deviceId
    }
  }
}
